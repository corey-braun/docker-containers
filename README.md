These files define the docker containers I currently use to run services at home.

Includes `docker-compose.yml` files defining main services (in root directory) and home assistant services (in homeassistant/).
